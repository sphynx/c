1.

➜  wish git:(master) make run
wish> path /usr/bin /bin
setting new path...
wish> wc wish.c
wc is not found in PATH                <--- does not work when `wc` above has been invoked with an argument
wish> wc
wc is not found in PATH
wish> wish> wish> %                    <--- multiple exits are required (it looks like children do not exit properly and we nest)
➜  wish git:(master) make run
wish> path /usr/bin /bin
setting new path...
wish> wc
resolved with PATH to: /usr/bin/wc     <--- now it works fine (when `wc` above has no args), it looks like arguments corrupt PATH?
abc
       1       1       4
wish> %
