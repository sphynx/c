/*
 * I've actually used the proposed method (i.e. breaking it
 * statistically) for challenge 19, so it does not make much sense to
 * repeat it here again, so I am just skipping this challenge.
 */
